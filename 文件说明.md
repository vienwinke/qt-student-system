# 文件说明（逐文件）

> 仓库：`vienwinke/qt-student-system` ｜ 分支：`main`
> 项目：C++ 学生成绩管理系统（C++17 + Qt Widgets + MySQL）

## 文件总览

| 文件 | 类型 | 职责 |
|------|------|------|
| `main.cpp` | 源码 | 程序入口 |
| `MainWindow.h/.cpp` | 源码 | 主窗口界面与业务逻辑 |
| `Student.h/.cpp` | 源码 | 学生模型 + 数据库 CRUD |
| `Score.h/.cpp` | 源码 | 成绩模型 + 数据库 CRUD |
| `Calculator.h/.cpp` | 源码 | 总分计算工具 |
| `StudentSystem.pro` | 工程 | qmake 构建配置 |
| `students.vcxproj(.filters)` | 工程 | Windows MSVC 工程 |
| `run_qt.sh` | 脚本 | Linux/WSL 启动脚本 |
| `.env.example` | 配置 | 环境变量示例 |
| `.gitignore` | 配置 | git 忽略规则 |
| `README.md` | 文档 | 项目说明 |

## 逐个文件说明

### 1. `main.cpp` — 程序入口
- 读取环境变量 `DB_PASSWORD`，未设置时使用默认密码 `123456` 并打印警告。
- 创建 `QApplication`，设置全局中文字体（文泉驿正黑）。
- 实例化 `MainWindow` 并进入 Qt 事件循环。

### 2. `MainWindow.h` — 主窗口头文件
- 继承 `QMainWindow`，声明 `Q_OBJECT`。
- 定义界面槽函数：`refreshStudents`、`refreshScores`、`addStudent`、`deleteStudent`、`addScore`、`deleteScore`、`queryStudent`。
- 成员：学生表格 `studentTable`、成绩表格 `scoreTable`（`QTableWidget`）。

### 3. `MainWindow.cpp` — 主窗口实现（界面 + 业务）
- 构建主窗口 UI，放置学生/成绩两个表格及相关按钮。
- 实现学生、成绩的添加 / 删除 / 查询逻辑，刷新后实时显示。
- 连接数据库失败时弹出 `QMessageBox` 错误提示。

### 4. `Student.h` — 学生模型声明
- `Student` 类：学号 `sid`、姓名 `sname`，含 getter/setter。
- 声明数据库函数：`getDBConn`、`closeDBConn`、`AddStudent`、`QueryStudentBySid`、`UpdateStudentName`、`DeleteStudent`。

### 5. `Student.cpp` — 学生模型 + 数据库 CRUD 实现
- `getDBConn()`：单例式 MySQL 连接，带 `mysql_ping` 重连检测，连接 `127.0.0.1` 的 `student_sys` 库，字符集 `utf8mb4`。
- `AddStudent`：INSERT 学生，所有字符串经 `mysql_real_escape_string` 转义防注入。
- `QueryStudentBySid`：按学号查询单条学生记录。
- `UpdateStudentName`：按学号改名。
- `DeleteStudent`：先删该生的成绩记录，再删学生（级联删除）。

### 6. `Score.h` — 成绩模型声明
- `Score` 类：学号、科目、平时分、期末分、总分，含 getter/setter。
- 声明 `AddScore`、`QueryScore`、`DeleteScore`。

### 7. `Score.cpp` — 成绩模型 + CRUD 实现
- `AddScore`：INSERT 成绩记录（含总分列）。
- `QueryScore`：按「学号 + 科目」查询单条成绩。
- `DeleteScore`：按「学号 + 科目」删除成绩。
- 同样对字符串参数做转义防注入。

### 8. `Calculator.h` — 计算工具声明
- 声明 `int CalculateFinalScore(int usual, int end, double weightA)`。

### 9. `Calculator.cpp` — 总分计算实现
- 公式：`总分 = weightA × 平时分 + (1 - weightA) × 期末分`，四舍五入返回整数。

### 10. `StudentSystem.pro` — qmake 工程文件
- 配置 Qt 模块（core/gui/widgets）、C++17、目标名 `StudentSystem`。
- 指定源码与头文件列表，链接 `-lmysqlclient`。
- 内含 Windows 下 MSVC + MySQL Connector 的注释示例。

### 11. `students.vcxproj` — Windows MSVC 工程
- Visual Studio 工程文件，配置 Debug/Release × Win32/x64 四种构建。
- 目标名 `student_ss`，Windows 平台版本 10.0。

### 12. `students.vcxproj.filters` — VS 文件筛选器
- Visual Studio 解决方案资源管理器的目录结构展示配置（源码/头文件分组）。

### 13. `run_qt.sh` — Linux/WSL 启动脚本
- 设置中文输入法环境变量（`GTK_IM_MODULE`、`QT_IM_MODULE`、`XMODIFIERS` = fcitx）。
- 设置图形显示（`DISPLAY=:0`、`XDG_RUNTIME_DIR=/mnt/wslg`，适配 WSLg）。
- 未设置 `DB_PASSWORD` 时默认 `123456`。
- fcitx 未运行则自动启动，最后进入程序目录运行 `./StudentSystem`。

### 14. `.env.example` — 环境变量示例
- 提供 `DB_PASSWORD` 环境变量的书写格式示例。

### 15. `.gitignore` — git 忽略规则
- 忽略编译产物（`*.o`、`Makefile`、`StudentSystem`、`moc_*` 等）、环境变量文件 `.env`、IDE 目录、Python 缓存。

### 16. `README.md` — 项目说明文档
- 项目简介、功能列表、技术栈。
- Linux/WSL 与 Windows 的安装、编译、运行步骤。
- 数据库表结构（student / score / subject_weight）与总分计算公式。